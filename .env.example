# Copy this file to .env and fill in your key.
# The project runs fully without a key — ai_insights.py falls back to a
# template summary if GEMINI_API_KEY is missing.

GEMINI_API_KEY=

# Optional: override the default model.
GEMINI_MODEL=gemini-1.5-flash
